# Changelog

## 0.5.0 - pending publish

- Add `precheck()` as a developer-facing SDK helper for execution evidence before delegation.
- Export `PrecheckInput`, `PrecheckPolicy`, `PrecheckResult`, `RankedCandidate`, and typed `Xaip*Error` classes from the package entrypoint.
- Document controlled `reason` strings, optional `decision` values, policy options, and unsupported `requireCoSignatureRatio > 0` behavior.
- No breaking API changes from `0.4.0`.
